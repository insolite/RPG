#include "StdAfx.h"
#include "Player.h"

Player::Player(std::map<std::string, std::string> strings, std::map<std::string, int> integers) : MapObject(strings, integers)
{
	
}

Player::~Player(void)
{
}
